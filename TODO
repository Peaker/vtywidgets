* Grid:

  * Attach (Weight, (MinSize, Maybe MaxSize)) and put Widget behind (Size ->).
    Allowing the grid to set the size more smartly, allowing fixed
    sizes, dynamic sizes, etc.
    * Have Grid take Size and live in that..
    * Should all Images guarantee that there's nothing outside their ClipRect?
    * Clip the subwidget images (Can be Image.clip :: Monoid a => IntersectRect -> Endo (Image a),
      returns mempty for any out-of-intersection position)

* Scroller:

  * Can take a (widget, focusArea) and make sure the focusArea is in
    range of the scroll

* TextEdit:

  * Arg for max num of lines
